class Object {
    public var alive: bool = false;
    public func __gc_mark() -> void {
        alive = true;
    }
}

interface Action {
    func perform() -> void;
}

func performAction(action: Action) -> void {
    action.perform();
}
