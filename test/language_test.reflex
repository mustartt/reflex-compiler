func distance2(x: int, y: int) -> int {
    var x2: int = x * x;
    var y2: int = y * y;
    return x2 + y2;
}

class Point {
    public var x: int;
    public var y: int;

    public func add() -> int {
        return x + y;
    }
}
